import React, { FC } from 'react';

// HOC 
import withMotion from '../HOC/MotionHOC';

const Skills: FC = () => {
    return (
        <div>
            
        </div>
    );
};

export default withMotion(Skills);